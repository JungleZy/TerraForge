# 统一文本文件以 LF 检出:Windows  runners 默认 core.autocrlf=true,
# CRLF 转换会改变文件字节数,使 static/vendor 的字节清单校验
# (tests/test_css_contract.py)在 Windows 上必然失败。
* text=auto eol=lf

# Windows 批处理保留 CRLF,避免老版本 cmd 解析问题。
*.bat text eol=crlf

# 二进制资源显式声明,不参与行尾转换。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.icns binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
*.terrain binary
*.tif binary
*.tiff binary
*.db binary

# 全球 base 地形的分卷(约 167MB,仓库里最大的二进制)。后缀 .partaa/.partab
# 不在上面任何一条里,现在只靠 git 的 NUL 字节启发式活着。而文档写明的装机流程
# 是 `cat base_z8.tar.gz.part* > base_z8.tar.gz` 再解压 —— 一旦被当文本做行尾
# 转换,拼出来的 gzip 校验和就不对,报错发生在用户机器上。
assets/terrain/base_z8.tar.gz.part* binary
