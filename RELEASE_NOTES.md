## ⚠️ 请先看这一条：0.0.9 及更早版本拼出来的 GeoTIFF，地理位置是错的

拼接大图时，瓦片边界的纬度用了线性插值计算，而 Web Mercator 的纬度是非线性的（`arctan(sinh(...))`）。后果是产物整体错位：

| 缩放级别 | 位置偏差 |
|---|---|
| z10 | 约 **14.8 米** |
| z6 | 约 **3.7 公里** |

单独看图看不出问题，**一旦拿去和其它地理数据叠加、量距离、做校准，结果就是错的**。

**如果你用 0.0.9 或更早的版本拼过影像，请把那些文件当作不可用，用本版本重新拼一遍。** 同时注意：新旧产物的几何不一致，任何按旧输出校准过的下游流程需要重新校准。

---

## 数据正确性修复

- **拼接产物的地理配准修正**（见上）。改为在 EPSG:3857 平面坐标下拼接，再投影回 EPSG:4326。默认输出坐标系不变，仍是 EPSG:4326。
- **彩色样式不再拼成灰白图。** roadmap / terrain 这类带调色板的瓦片，此前拼接时会丢掉颜色表、把调色板索引当成灰度值渲染。
- **「仅拼接图」输出选项从必崩变可用。** 此前选这一项一定抛异常。
- **改过选框范围后，提交的是新范围。** 此前拖动顶点调整框选后，提交的仍是调整之前的范围——下载到的区域和你看到的不一致。
- **拼接失败不再被报告成「已完成」。** 失败原因会写进任务记录并显示出来；部分缩放级别失败时也会明确标注是哪几级。
- **瓦片写入改为原子操作。** 此前中间文件非原子写，拼接时可能静默丢失瓦片（实测出现过 32×16 的网格只拼出 18×14，全程只有一条警告）。

## 界面

- **深色主题**，全站控件、表单、弹窗、地图控件统一。
- **进度条按任务状态着色，并显示真实进度。** 此前进度条的过渡时长（0.6 秒）远长于后端推送间隔（每秒 10~20 次），导致它永远从上一帧位置重新起步——实测真实进度 80% 时，条上显示的是 23%。
- **失败的任务保留在列表里**并展开错误原因，失败提示不再自动消失。此前失败卡片会直接消失。
- **1366×768 小屏上「创建下载任务」按钮不再被挤出屏幕。** 控件高度从 43.7px 收到 28px，框选后的四至显示从 5 行压成 2 行。
- **禁用的按钮一眼能看出点不动。** 此前禁用态仍是饱和的蓝色，看起来完全可点——而这个按钮默认就是禁用的（要先框选才解除）。
- **表格里的错误文字是红色的。** 此前「加载失败」被单元格样式压成了和正常内容一样的灰白色。
- **弹窗打开时背景会正常变暗。** 此前遮罩层的背景色被一条全局样式压成了全透明，从未生效过。
- 六种任务状态（等待/运行/暂停/完成/失败/取消）在状态徽章、卡片边条、地图矩形描边三处都有各自的颜色。
- 支持系统的「减少动态效果」偏好设置（`prefers-reduced-motion`）。

## 断网 / 内网可用

- **8 个前端库全部内置**（Bootstrap、Leaflet、Leaflet.draw、Socket.IO、Inter 与 JetBrains Mono 字体），不再请求任何 CDN。此前断网时弹窗、下拉、绘制工具条的图标会整体失效。
- 唯一仍需联网的是**地图底图**（OpenStreetMap）。

---

## 已知限制

- **底图仍走公网。** 完全断网时地图区域是深色空白，但选框和下载功能正常。
- **拼图过程中的临时磁盘占用约为旧版的 3 倍**（拼完自动清理）。拼十万级瓦片的大任务前，请确认磁盘有富余。
- **中文字体走系统自带。** Windows / macOS 正常；放在没装中文字体的 Linux 或容器里运行时，中文会显示成方块。
- **选「等高线瓦片」类型时**，1366×768 这类小屏上提交按钮在首屏之外，需要把右侧面板往下滚一点。
- **拼接时如果缓存丢的是网格中间的瓦片**（不是边缘），程序目前发现不了，会拼出一块黑色区域并报告成功。触发条件很苛刻（两个范围重叠的任务同时拼接），旧版本同样存在且更严重。拼完的大图建议扫一眼有没有黑块。

## 升级说明

数据库无结构变更，0.0.9 的现有数据可直接沿用。
