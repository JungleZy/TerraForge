## v0.2.8 —— 地形的三个静默失败修复 + 逐顶点法线与光照开关（已有地形瓦片必须全部重切）

**先说结论：已经切好的地形瓦片必须全部重切，否则这一版对你没有任何效果。** 两个互相独立的原因，任何一个单独成立都要求重切。

**原因一：此前的地形瓦片 Cesium 根本解不出来 —— 地形从未真正渲染过**
- quantized-mesh 里的 `triangleCount` 字段被写成了索引元素数（正确值的 3 倍），读端按它去取索引直接越界报错。全程静默：瓦片请求全部返回 200、任务标记完成、页面不报错，只是地形出不来。
- 这一版修好了**写瓦片的代码**，但**已经写在磁盘上的瓦片仍然是坏的** —— 那个错误的数字就在瓦片字节里。

**原因二：这一版往瓦片里新增了法线数据与择优三角化**，旧瓦片里没有这些内容。

**另一个静默失败：地形高程整体是错的**
- `layer.json` 里的 `parentUrl` 默认值以 `/layer.json` 结尾，而 Cesium 把这个值当**目录**用（自己再拼一次 `layer.json`），于是请求 404。Cesium 对这个 404 不报错，而是塞进一个假的高程图图层，并且这个错误假设会污染整个地形数据源 —— 连任务自己的瓦片也被按错误格式解析。
- 实测：4154 米的山峰被解成海平面以下 744 米，而页面一切正常、控制台一条错误都没有。
- 已修，但**同样只对新切的瓦片生效**：存量 `layer.json` 里写死的还是坏值。配置表里那一行也仍是旧值（这一版改的是新建数据库的默认值，不动已有的行），不过写 `layer.json` 时会统一规整一次，所以重切就能兜住，不需要手动去改配置。

**逐顶点法线与地形光照开关**
- 每张瓦片额外写入逐顶点法线（每个顶点 2 字节）。地图工具栏多了一个「光照」按钮，**默认关闭**：它是纯渲染端开关，开关不需要重切瓦片。开着会与卫星影像自带的光照叠加，未必好看，所以数据先备着、什么时候开由你决定。这个偏好只存在浏览器本地，不进配置表。
- ⚠️ 全球底图 `downloads/terrain/base_z8/` 是老产物，里面没有法线。开光照后它覆盖的低层级区域会显示为近乎纯黑的一块 —— 要消除得把底图也重切一遍。

**三角化：逐瓦片挑更小的那一个**
- 新增了自适应三角化（Martini/RTIN），能按地形起伏把三角形数减掉八成。但**不是无条件用它**：实测 11 万张配对瓦片，压缩后山地反而涨 17.6%、丘陵涨 9.8%，只有平缓地形省 54.2%。原因是瓦片以压缩形式落盘和传输，而压缩后规则网格每个三角形只占约 0.91 字节、Martini 要约 4.04 字节，贵 4.4 倍 —— 减面必须超过 77% 才能在体积上打平，山地做不到。
- 所以最终做法是：每张瓦片两种都编一遍，取压缩后更小的那一个。**单张瓦片的体积不可能比原来更差。**
- 高程精度一步没让：误差预算参数保持原值，没有为了省体积去放宽它。

**代价**
- **切片 CPU 增加约 36%**（算法线 + 两种三角化各跑一遍）。换来的是法线数据与逐瓦片最优的字节数。
- 瓦片体积因法线而增大：压缩后规则网格分支 +55.4%、Martini 分支 +22.5%。

**验证**
- 1103 项测试全部通过。
- 浏览器端实测：真实 DEM 切片后经 HTTP 取回，Cesium 解出的高程与源 DEM 交叉验证 —— 源 DEM 真值 2672 / 1086 / 4154 米，Cesium 读到 2656.6 / 1092.3 / 4154.2 米；法线段完整，长度与顶点数逐张对得上。

---

## 通用说明

- **下载安装**：从下方 Assets 下载对应平台压缩包（`terraforge-windows.zip` / `terraforge-linux.tar.gz` / `terraforge-macos.tar.gz`），解压即用，无需安装 Python 环境。
- **首次运行**：启动可执行文件后，浏览器访问 http://localhost:5000 ；代理、并发、缓存管理等在「配置」页修改。
- **历史版本**：完整更新历史见仓库 [CHANGELOG.md](https://github.com/JungleZy/map-download/blob/master/CHANGELOG.md)。
- **使用文档**：见仓库 [README.md](https://github.com/JungleZy/map-download/blob/master/README.md) 与 [docs/guides/QUICKSTART.md](https://github.com/JungleZy/map-download/blob/master/docs/guides/QUICKSTART.md)。
