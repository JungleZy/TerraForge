## v0.3.4 —— 英文界面排版修了一轮；缓存分类名会跟语言走；/config 底部按钮不再悬空

**先说结论：这一版没有任何功能变化，修的全是界面 —— 英文模式下左上工具条、框选浮层、配置面板三处排版错位；「缓存管理」的分类名在英文界面不再冒出中文；/config 独立页底部的「重置/保存」按钮不再悬空；缓存列表里长分类名折行时，尺寸读数不再被挤断。已下载的数据、配置、历史全部照旧，没有数据库迁移，升级后不需要做任何事。**

**英文模式下三处排版错位（中文界面一个像素没动）**
- **左上工具条**：此前英文下每颗按钮各自量各自的宽（Zoom in 62px、Zoom out 70px），按钮之间的分隔线、悬停底色参差不齐；更糟的是胶囊圆角在更宽的英文工具条上变成 36px 的半圆端头，把首尾按钮的标签啃掉字母（「Zoom out」变「Zoom ou」）。现在全组按钮统一拉齐到最宽标签的宽度，圆角与中文端头用同一段弧。
- **框选浮层**：框选之后右上角的「Download」「Delete」按钮会被裁掉尾字母 —— 浮层宽度封顶，英文按钮加提示一行装不下时，排版引擎先去压按钮。现在按钮钉死自身宽度，装不下就让提示整条换到第二行。
- **配置面板**：英文标签比中文长，三栏各 149px 放不下就折行，同一排里有的标签一行有的两行，输入框上下错位 21px。现在分栏按面板实际宽度铺：默认宽度下落成两栏（标签全部回到一行，输入框反而更宽），面板拖窄会落成一栏；/config 独立整页仍是三栏，与之前逐像素相同。
- 顺带修了一颗同毛病的按钮：图源向导里的「Analyze」在窄面板里被挤掉 4px。

**「缓存管理」在英文界面不再冒中文**
- 此前缓存分类名（「DEM 缓存」「瓦片缓存（satellite · 1a2b3c4d）」）是写死在服务端的中文，英文界面照原样显示。现在分类名跟界面语言走：英文下显示「DEM cache」「Tile cache (satellite · 1a2b3c4d)」。中文显示逐字未变。
- 缓存列表的排版也顺手修了：分类名太长折成两行时，右侧的「581.6 KB · 39 files」此前会跟着断在数字和 files 之间（中英文都有这毛病）。现在尺寸读数和「清理」按钮整段钉死不换行，只有分类名允许折行。

**/config 独立页底部按钮不再悬空（中英文同形的老问题）**
- 此前底部操作条用的是页面底色加一条几乎看不见的分隔线：上面是滚到一半被裁掉底边的卡片，下面是给状态栏胶囊让位的空白，「重置为默认值」「保存配置」两颗按钮像悬在半空。现在它是一条看得见的浮层操作条（与滑出面板同级的底色、卡片同款描边圆角、右缘与上面卡片对齐）。滑出面板里的形态本来就是对的，没有动。

**历史统计卡的一个小改动**
- 英文下「累计下载量」那张卡此前写「Total downloaded」，现在只写「Total」。中文「累计下载量」不变。

---

**给排障和构建的人**

- 英文专属样式集中在 `static/css/style.css` 的 `html[lang="en"]` 选择器下（服务端按 cookie 渲染 `<html lang>`），中文规则路径零改动。缓存分类名走 `api.cache.category.*` 四条 i18n 键，括号（全角/半角）写在译文里而不是代码拼。
- 一处有意的妥协：CSS 里该用子组合符 `>` 的地方用了后代组合符 —— `tests/test_css_contract.py` 的按钮层叠模型遇到子/兄弟组合符会把十条按钮断言判成「模型已失效」，后代写法在这份 markup 里选中的是同一批元素。

**验证**

- 全部修改都在真实浏览器里逐元素量过（1440×900 与 1280×720 两种视口）：错位量、裁字宽度、修后对齐位置均为实测值，中文界面逐项回归未变。
- 全量测试 **2639 项通过 / 3 项跳过**（开发机 Linux，2 分 38 秒；跳过的只在特定平台上有意义）。覆盖率数字不在本机这轮里 —— 三平台 CI 构建会带 `--cov-fail-under=78` 再各跑一遍。
- 契约测试：`tests/test_css_contract.py`（按钮层叠模型、字号棘轮、!important 上界）、`tests/test_cache_management.py`（缓存标签中文原文断言）、`tests/test_i18n.py`（中英双语齐全性）等全部通过。

---

## 通用说明

- **下载安装**：从下方 Assets 下载对应平台压缩包（`terraforge-windows.zip` / `terraforge-linux.tar.gz` / `terraforge-macos.tar.gz`），解压即用，无需安装 Python 环境。
- **下载体积**：每个平台仍包含 167 MB 的全球底图分卷（自 v0.2.8 起）。
- **首次运行**：启动可执行文件后，浏览器访问 http://localhost:5000 ；代理、并发、缓存管理等在「配置」页修改。程序另会监听 5001 出瓦片，不放行也能用。
- **许可证与第三方声明**：程序目录下的 `LICENSE`（MIT）与 `THIRD_PARTY_NOTICES.md`。MIT 只覆盖软件代码，**不授予**任何数据与在线服务的使用权。
- **历史版本**：完整更新历史见仓库 [CHANGELOG.md](https://github.com/JungleZy/TerraForge/blob/master/CHANGELOG.md)。
- **使用文档**：见仓库 [README.md](https://github.com/JungleZy/TerraForge/blob/master/README.md) 与 [docs/guides/QUICKSTART.md](https://github.com/JungleZy/TerraForge/blob/master/docs/guides/QUICKSTART.md)。
