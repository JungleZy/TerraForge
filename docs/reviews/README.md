# docs/reviews —— 带日期的时点审查快照

> **每一份都是「撰写当天」的代码状态，没有一份代表当前实现。** 想知道今天怎么跑，看仓库根 `README.md` + `CLAUDE.md` + 代码本身。

## 读之前必须知道的两条

1. **报告里的「已修复 / 已处理完毕 / 已上线」只对撰写当天有效。** 那是作者在那一天核实的结论，不是持续维护的状态位。代码此后一直在动，同一处可能被改回去、被重构掉、或被新问题覆盖。任何一条要当依据用，都得回代码验一遍。
2. **正文不回改。** 报告里的 `file:line`、测试通过数、版本号、目录路径全是当日快照。改掉等于伪造审查记录，所以一律保持原样——文件内部指向已移动位置的路径是有意保留的历史原貌，不是待修死链。

## 清单（按时间倒序）

| 文件 | 日期 | 它的价值 |
|---|---|---|
| `2026-08-15-frontend-design-audit.md` | 2026-08-15 | 前端设计审查第二轮（Dieter Rams 十条），基线 `81813c7`。5 个只读取证代理 + **编排者独立复核四项承重数字**（gzip、键盘框选、徽章对比度、陈旧读数）。**总分 13/30（上轮 9/30），裁定仍是 REDESIGN——但重做对象换成「交付层 + Bootstrap 继承边界 + 术语/状态自洽」，信息架构与几何令牌本轮实测已达标，明确列为 anti-scope。** 上一轮判的病逐条复测：间距刻度成型（首屏值集合 `{1,2,8,12}`，落外 6 值各有就地登记）、任务行 19 颗按钮同高 28px、四条管线进一张非模态面板、Escape 后四张面板焦点全部归还触发钮。**#9 从 1 掉到 0 是测量口径变化不是回归**：实测首屏 JS **6.51 MiB**（Cesium.js 单文件 5.64 MiB = 86.6%）且**服务端一个字节没压**（`curl -H 'Accept-Encoding: gzip, br'` 响应无 `Content-Encoding`），上轮按「500KB–2MB」档给的 1 分不成立。三条本轮新发现的承重缺陷：**瓦片预估不跟着选区走**（`updateTileEstimate()` 8 个调用点里没有一个是选区变化路径，实测清除选区后屏上同时存在「请在地图上框选下载区域」与「预计 25,545 张瓦片」）、**键盘无法画框**（`map.js:1734/1769/1827` 三个回调全绑鼠标，13 项动作 12 项键盘可达就差这一项）、**令牌只接管前景不接管背景**（全站真实文字对比度失败只有 2 处，两处同一个根因；快捷键徽章 1.27:1 **第三轮未修**）。文案诚实性底子干净：夸大宣传零命中、黑暗模式零命中、破坏性确认默认焦点在取消；但标签↔行为不符仍有 5 条。末尾附 8 条「无法查证」（失败任务外观、真机 TTI 等）与逐条评分差量表。 |
| `2026-08-14-frontend-design-audit.md` | 2026-08-14 | 前端设计审查（Dieter Rams 十条），基线 `967d498`（v0.3.5）。6 个只读代理取证 + **真实浏览器实测三档视口、明暗双主题、11 张截图**。**总分 9/30，裁定 REDESIGN——但重做对象是「系统层 + 信息架构」，不是像素。** 三个用户感觉逐条落到根因：样式不统一 = 颜色层满分（0 处硬编码色、明暗 100% 对等）而**几何层无刻度**（间距令牌只有 2 个、9 条声明消费、31 个离散字面量、33 个按钮类 5 套几何）；操作不方便 = **四条管线分处两个弹窗且两条入口藏在任务面板筛选行**（`index.html:39-41` 自认）；不专业 = 12 套显隐机制 / 15 处重复入口 / 任务面与配置面各存在两份。最要紧的工程约束写在开头：**530 条前端契约断言（css 292 + tasks_js 238，当日基线 224 passed 全绿）把 CSS 形状钉成了公开契约，归一化会红约 82 条，其中 2 条是模型级封锁（禁子组合符、禁 `@media` 内 `color`），必须先扩测试模型再动 CSS**，否则重演 `CHANGELOG.md:32` 那次妥协。另含一天可修完的 12 项小改（快捷键徽章 1.31:1 看不见、`/history` 白付 5.9MB Cesium、20Hz 后台空闲循环等）。 |
| `2026-08-09-full-project-review.md` | 2026-08-09 | 全项目评审，15 个并行分片 + 主评审复核 + **真实浏览器驱动**（补上了上一份自己点名的缺口）。**143 条发现：2 P0 / 26 P1 / 66 P2 / 49 P3**，基线 `e9af408`（v0.2.14），**1963 passed / 3 skipped**。上一轮 4 P0 + 17 P1 逐条复查**零回归**（五条做了实测复验）。两条 P0 都是「静默产出错数据」，且都是**旧闸门判据不够强**：一条把「有交集」当成「有数据」（边界瓦片 99.5% 是钳位外推值却被声明 `available`），一条把「文件在」当成「文件对」（部分下载失败仍照常拼接，恢复时短路保留残缺 mosaic）。26 条 P1 全部落在六个模式上，没有孤立项。**首次做变异实测**：`test_terrain_stage_progress.py` 的 4 个生产闭包变异 **4/4 存活**——那三条用例断言的是测试文件里手抄的一份副本。浏览器实测确认两条产品硬约束在运行期成立（101 个请求全部同源、55 张底图瓦片全走 `/basemap`），并当场证伪了三处未经验证的 UI 断言。安全按 2026-08-08 用户裁定整体划出范围。 |
| `2026-08-08-full-project-review.md` | 2026-08-08 | 全项目评审，10 个并行分片 + 主评审逐条复核，**当日全部修完**：4 条 P0、17 条 P1、全部 P2、安全姿态第 1/3 项，1504 → **1757 passed**（`fork()` 的 DeprecationWarning 15 → 0）。每条都做过变异校验（把实现换回旧写法，用例必须变红）。安全部分**无新发现**——价值在于指出下面 2026-07-31 那份「已知并接受」的安全清单**已经过期**：决策后 4-8 天新增了任意文件读（`/terrain/base`）与全读型 SSRF（`/basemap` 转发）两个更强的接收面，清单该从 2 项更新为 5 项，**待重签**（唯一未做项）。另含三条自我更正：P1#5 的数据丢失链经实测不可达；`.btn-info` 不是可删死代码；fork→spawn 的否决被推翻并用真进程池验证。 |
| `2026-08-03-docs-audit.md` | 2026-08-03 | 文档审计。`docs/` 下全部 **55 份**逐份对照代码给出处置（4 删 / 13 改 / 29 归档），并附高风险清单与死链汇总。**本次 docs/ 重构就是照它执行的**——目录结构、状态头、归档约定都出自这份。 |
| `2026-08-03-full-project-review.md` | 2026-08-03 | 代码审查，11 维度 + 对抗性验证。**46 个真问题 / 4 个 high / 0 critical**。最值钱的是末尾的「被推翻的误报清单」：45 条送验发现没有一条被整条推翻，但有 **16 处次生结论被证伪**并逐条记下了实际情况——下次审查撞上同样的推论可以直接跳过，不用重走弯路。 |
| `2026-07-31-code-only-review.md` | 2026-07-31 | 只以代码为准的全项目审查（7 个只读代理通读约 1.5 万行，忽略一切 docs/注释的自我声明）。同时确立了沿用至今的**部署前提**：程序跑在可信环境，零鉴权 / 绑 `0.0.0.0` / CORS `*` / 凭据明文均为已接受的设计决策，不计入缺陷。 |
| `2026-07-29-swarm-review.md` | 2026-07-29 | 14 个并行代理分模块审查后去重合并。含**用户裁决记录**——「cancel 不得改写终态」「删除任务是否清磁盘」这些今天写进 `CLAUDE.md` 的约定，源头在这里。 |
| `2026-07-27-ui-review.md` | 2026-07-27 | 界面与操作专业度评审（90 条发现，24 条通过对抗性验证），是 `docs/superpowers/plans/2026-07-27-*` 那三份改造计划的依据。含 5 张实测截图，存放在 `../assets/images/ui-review-2026-07/`。 |
| `2026-05-07-mvp-completion.md` | 2026-05-07 | MVP 时点快照，只有回溯价值。它描述的是**单管线 + Leaflet + venv** 的形态，与今天（四条并行管线 / CesiumJS / uv）差一个数量级；「如何使用」一节的启动指令会直接失败。 |
| `2026-05-08-comprehensive-review/` | 2026-05-08 | **流产的审查**：计划产出 10 份报告，只落地 1 份（`04-backend-architecture.md`）。目录内已有 README 说明编号为什么从 04 开始，以及它的多条 CRITICAL 为什么被判「按设计不做」。 |

## ⚠️ 2026-07-31 那份的「全部处理完毕」不能全信

`2026-07-31-code-only-review.md:11` 写着「全部 HIGH、MEDIUM、LOW 项及已知遗留①-④均已处理完毕」。**其中 MEDIUM #21 当时没有收敛**：测试隔离虽然新增了 `conftest` 的 `fresh_import` 工具，但各测试文件里手写的 `sys.modules.pop` 清单并没有整批迁过去。三天后 `2026-08-03-full-project-review.md` 的 **M23** 以「〔上轮已提出仍未修〕」重新提出了同一条。

**这一条现在从「数文件」改成了棘轮，判据在 `tests/test_conftest_isolation_contract.py`。** 它不要求把所有手写 pop 都迁走——上一轮试过在 conftest 里全局施加恢复语义，直接打红 15 条——而是钉住真正会出事的那个组合：某个模块被裸 `pop`（不恢复）的同时又被别的文件在**模块级** `from` 导入，于是同一个类存在两份对象，`pytest.raises` / `except` / `isinstance` 随文件执行顺序时绿时红。名单 `KNOWN_DOUBLE_INSTANCE_RISKS` 已清空，两侧都有断言：新增这种组合会红，名单里留着已修条目也会红。

所以这里不再写「`tests/` 下还有多少个文件手写清单」。那个数字每周都在变（本 README 曾写 48，同日实数 56），而且它从来不是判据——判据是上面那个文件跑不跑得过。

## 当前事实源

| 想知道什么 | 看哪里 |
|---|---|
| 功能总览、版本 | 仓库根 `README.md` |
| 架构、四条管线的分工、目录约定 | 仓库根 `CLAUDE.md` |
| 构建 / 安装 / 快速上手 / 分发 | `../guides/` |
| 当前实现说明 | `../reference/` |
| 历史实施计划与设计稿 | `../superpowers/` |
