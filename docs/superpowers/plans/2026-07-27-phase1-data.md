# Phase 1：数据正确性 Implementation Plan

> **归档文档 · 非当前实现**
> **记录时间**：2026-07-27 ｜ **状态**：已实施（2026-07-28 落地，合并于 `44788878f`），内容与今天的代码基本一致
> **勿当待办重跑**：测试早已全绿，重跑只会白花一轮排查时间，或把已经修好的代码改回中间态。
> 已失效的细节：Task 6 的 `image_only` 语义表被 0.2.4 的「边下边复制」改动推翻，**以代码为准**；所有 Leaflet.draw 选择器与事件名在今天的 CesiumJS 前端里已无作用对象；:851 提到的「PyInstaller 离线打包形态」现为 Nuitka；正文里的测试数（基线 148 → 收尾 162 passed）是当日快照，现全量 900 passed。
> ⚠️ 复选框状态无效（54 个全未勾 ≠ 未执行，正文仍是「逐任务实施」口吻）；正文源码与行号为当日快照，禁止照抄或照行号定位。
> *正文保持原样未回改。*

---

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 逐任务实施。步骤用 `- [ ]` 复选框跟踪。

**Goal:** 让工具产出的 GeoTIFF 地理配准正确，界面上不再有必崩的选项，框选范围所见即所得。

**Architecture:** 三条独立的修复线。(1) 把瓦片配准从「错误的等纬度线性近似」改成「Web Mercator 平面坐标」，并在拼接末尾用真正的重投影输出 EPSG:4326，保持存量用户的输出坐标系不变。(2) 补全 `OutputFormat` 枚举并理清 `both`/`tiles_only`/`image_only` 三种语义。(3) 修 `map.js` 的绘制事件监听与表单重置。

**Tech Stack:** Python 3.12 · GDAL 3.8.4 · pytest · 原生 JS + Leaflet.draw 1.0.4

**上级计划：** [`2026-07-27-master-plan.md`](2026-07-27-master-plan.md) —— **Global Constraints 一节对本计划全部任务生效，开工前必读。**

---

## Global Constraints（本阶段补充）

在主计划的全局约束之上，本阶段额外要求：

- **`_add_georeference` 有缓存短路**（`download_engine.py:766`：`if georef_path_obj.exists(): return georef_path`）。改了配准算法后，磁盘上残留的旧 `_geo.tif` 会被直接复用，**导致修复静默失效**。每次手工验证前必须先清掉 `cache/` 下所有 `*_geo.tif`：
  ```bash
  find cache/ -name '*_geo.tif' -delete
  ```
- **不改变默认输出坐标系**。修复后默认产出仍是 EPSG:4326 的 GeoTIFF，只是配准从「错的」变成「对的」。EPSG:3857 直出作为可选项，但本阶段**不做 UI**（留给第二档）。
- **`png`/`jpg` 两个 `output_format` 值对产物没有任何影响**（实测：`task_manager.py:909` 输出路径硬编码 `.tif`，`download_engine.py:674-676` 按扩展名选驱动，永远走 GTiff）。它们是遗留 DB 值，只作为 `image_only` 的同义词兼容，**不要声称支持 PNG/JPG 输出**。

---

## File Structure

| 文件 | 责任 | 本阶段改动 |
|---|---|---|
| `services/download_engine.py` | 瓦片下载、配准、拼接 | 抽出 geotransform 纯函数；改配准到 3857；拼接末尾加 warp |
| `models/task.py` | 数据模型与枚举校验 | `OutputFormat` 补 `IMAGE_ONLY` |
| `services/task_manager.py` | 任务生命周期编排 | 修正三种输出格式的分支语义 |
| `templates/index.html` | 主表单 | 无改动（`image_only` 选项保留，改为真正可用） |
| `static/js/map.js` | 地图绘制与表单提交 | 补 `EDITED` 系列监听；抽 `resetForm()`；修按钮状态 |
| `tests/test_tile_georeference.py` | **新建** | geotransform 纯函数的数学正确性 |
| `tests/test_output_format.py` | **新建** | `OutputFormat` 枚举与三种语义 |
| `tests/test_map_js_contract.py` | **新建** | `map.js` 关键契约的文本级回归保护 |

---

## Task 1：抽出 geotransform 纯函数（纯重构，行为不变）

**Files:**
- Modify: `services/download_engine.py:760-808`（`_add_georeference` 内的 geotransform 计算段）
- Test: `tests/test_tile_georeference.py`（新建）

**Interfaces:**
- Consumes: 无
- Produces: `DownloadEngine.tile_geotransform(tile: Tile, width: int, height: int) -> tuple[list[float], int]` —— 返回 `(geotransform_6元组list, epsg_code)`。Task 2/3 依赖此签名。

**Rationale:** 当前 geotransform 计算埋在 `_add_georeference` 里，要测它必须先有 GDAL 打开一张真 PNG。抽成纯函数后可以脱离 GDAL 做数学断言，这是后续所有配准测试的基础。

- [ ] **Step 1: 写一个锁定当前行为的测试**

创建 `tests/test_tile_georeference.py`：

```python
"""
Tile georeference geotransform tests
"""

import os
import sys
import math

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from services.download_engine import DownloadEngine
from models.task import Tile


def _tile_lat(y_tile_float: float, zoom: int) -> float:
    """逆墨卡托：给定连续的瓦片 y 坐标，返回其真实纬度（度）"""
    n = 2 ** zoom
    return math.degrees(math.atan(math.sinh(math.pi * (1.0 - 2.0 * y_tile_float / n))))


def test_tile_geotransform_corners_match_tile_bounds():
    """geotransform 的左上角必须落在瓦片理论边界上（这一点当前实现就是对的）"""
    engine = DownloadEngine()
    tile = Tile(task_id=0, zoom=10, x=843, y=387)

    gt, epsg = engine.tile_geotransform(tile, 256, 256)

    assert len(gt) == 6, "geotransform 必须是 6 元组"
    assert gt[2] == 0 and gt[4] == 0, "north-up 影像的旋转项必须为 0"
    assert gt[5] < 0, "pixel_height 必须为负（图像 y 向下）"
    assert isinstance(epsg, int)
```

- [ ] **Step 2: 跑测试确认失败**

```bash
uv run pytest tests/test_tile_georeference.py -v
```

期望：`AttributeError: 'DownloadEngine' object has no attribute 'tile_geotransform'`

- [ ] **Step 3: 抽出纯函数，保持算法完全不变**

在 `services/download_engine.py` 的 `_add_georeference` **之前**插入新方法（`_add_georeference` 定义在 727 行，插在它前面）：

```python
    def tile_geotransform(self, tile: Tile, width: int, height: int):
        """
        Calculate GDAL geotransform + EPSG code for a single tile.

        Args:
            tile: Tile object with zoom/x/y
            width: Tile image width in pixels
            height: Tile image height in pixels

        Returns:
            (geotransform, epsg_code) where geotransform is
            [top_left_x, pixel_width, 0, top_left_y, 0, pixel_height]
        """
        n = 2 ** tile.zoom
        lon_min = tile.x / n * 360.0 - 180.0
        lon_max = (tile.x + 1) / n * 360.0 - 180.0

        lat_max = math.degrees(math.atan(math.sinh(math.pi * (1.0 - 2.0 * tile.y / n))))
        lat_min = math.degrees(math.atan(math.sinh(math.pi * (1.0 - 2.0 * (tile.y + 1) / n))))

        pixel_width = (lon_max - lon_min) / width
        pixel_height = -(lat_max - lat_min) / height

        geotransform = [lon_min, pixel_width, 0, lat_max, 0, pixel_height]
        return geotransform, 4326
```

然后把 `_add_georeference` 里 782-808 行那段（从 `# Calculate geographic bounds using Web Mercator formulas` 到 `]` 结束）**整段替换**为：

```python
        # Calculate geotransform (see tile_geotransform for the math)
        geotransform, epsg_code = self.tile_geotransform(tile, width, height)
```

并把 834-836 行的：

```python
        srs = osr.SpatialReference()
        srs.ImportFromEPSG(4326)
```

改为：

```python
        srs = osr.SpatialReference()
        srs.ImportFromEPSG(epsg_code)
```

- [ ] **Step 4: 跑测试确认通过，并确认没破坏既有测试**

```bash
uv run pytest tests/test_tile_georeference.py -v
uv run pytest tests/ -q
```

期望：新测试 PASS；全量 **149 passed**（原 148 + 新增 1）。

- [ ] **Step 5: 提交**

```bash
git add services/download_engine.py tests/test_tile_georeference.py
git commit -m "refactor(download): 抽出 tile_geotransform 纯函数,为配准修复做准备

geotransform 计算原本埋在 _add_georeference 里,要测它必须先用 GDAL 打开
真实 PNG。抽成纯函数后可脱离 GDAL 做数学断言。本次算法完全不变,仅移动位置。"
```

**Risks:**
- `math` 已在 `download_engine.py` 顶部 import（实测确认），不需要新增 import
- `Tile` 是 dataclass，`Tile(task_id=0, zoom=10, x=843, y=387)` 可直接构造（`status`/`retry_count` 有默认值）

---

## Task 2：写出证明当前配准错误的失败测试

**Files:**
- Test: `tests/test_tile_georeference.py`（追加）

**Interfaces:**
- Consumes: `tile_geotransform(tile, width, height) -> (gt, epsg)`（Task 1）
- Produces: 无

**Rationale:** 修之前必须先有一个红灯，证明缺陷真实存在、且量级和评审说的一致。没有这一步，修完无法证明修对了。

- [ ] **Step 1: 追加暴露缺陷的测试**

在 `tests/test_tile_georeference.py` 末尾追加：

```python
def test_tile_geotransform_interior_pixel_latitude_is_accurate():
    """
    瓦片内部像素的纬度必须与 Web Mercator 真值一致。

    当前实现把 (lat_max - lat_min) 线性均分给 height 行像素，但瓦片纵向
    是 Mercator y 等间隔,对应纬度是 atan(sinh(...)) 曲线。误差在瓦片
    中部最大、上下边界为 0。
    """
    engine = DownloadEngine()
    zoom, x, y = 10, 843, 387          # 北京附近
    height = 256
    tile = Tile(task_id=0, zoom=zoom, x=x, y=y)

    gt, epsg = engine.tile_geotransform(tile, 256, height)
    top_left_y, pixel_height = gt[3], gt[5]

    max_err_deg = 0.0
    for row in range(height + 1):
        if epsg == 4326:
            got_lat = top_left_y + row * pixel_height
        elif epsg == 3857:
            merc_y = top_left_y + row * pixel_height
            got_lat = math.degrees(
                2 * math.atan(math.exp(merc_y / 6378137.0)) - math.pi / 2
            )
        else:
            raise AssertionError(f"未预期的 EPSG: {epsg}")

        true_lat = _tile_lat(y + row / height, zoom)
        max_err_deg = max(max_err_deg, abs(got_lat - true_lat))

    max_err_m = max_err_deg * 111320.0
    assert max_err_m < 1.0, (
        f"瓦片内像素纬度最大偏差 {max_err_m:.2f} m,超过 1 m 容差。"
        f"(zoom={zoom}, y={y})"
    )
```

- [ ] **Step 2: 跑测试，确认它以正确的理由失败**

```bash
uv run pytest tests/test_tile_georeference.py::test_tile_geotransform_interior_pixel_latitude_is_accurate -v
```

期望：**FAIL**，断言信息形如 `瓦片内像素纬度最大偏差 14.8 m，超过 1 m 容差`。

**这个数字必须在 10~20 m 之间。** 如果差得远，说明测试写错了，先查测试再改实现。

- [ ] **Step 3: 提交这个红灯测试**

```bash
git add tests/test_tile_georeference.py
git commit -m "test(download): 加测试证明瓦片内像素配准偏差 14.8m

z10/y387 瓦片内像素纬度用线性均分近似,与 Web Mercator 真值最大差
14.8m。此测试当前失败,由下一个 commit 修复。"
```

> 允许提交失败的测试——这是 TDD 的红灯步骤，下一个任务立刻转绿。如果你的工作流不允许提交红灯，把 Task 2 和 Task 3 合并成一次提交。

**Risks:**
- 测试里 `epsg == 3857` 的分支现在走不到，但 Task 3 改完就会走到。先写好避免 Task 3 再改测试（改测试来迁就实现是 TDD 大忌）
- `111320.0` 是赤道上 1 度纬度的近似米数，用于把角度误差换算成距离量级，不需要精确

---

## Task 3：把配准改为 EPSG:3857

**Files:**
- Modify: `services/download_engine.py`（`tile_geotransform` 方法体）
- Test: `tests/test_tile_georeference.py`（已有）

**Interfaces:**
- Consumes: `tile_geotransform` 签名（Task 1）
- Produces: `tile_geotransform` 现在返回 EPSG:3857 的平面坐标 geotransform。Task 4 依赖这一点。

- [ ] **Step 1: 用平面坐标重写 geotransform**

把 Task 1 里加的 `tile_geotransform` 方法体**整个替换**为：

```python
    def tile_geotransform(self, tile: Tile, width: int, height: int):
        """
        Calculate GDAL geotransform + EPSG code for a single tile.

        Tiles are Web Mercator (EPSG:3857) squares of constant size at a given
        zoom level. Writing them in 3857 plane coordinates makes every pixel
        exactly the same size, so BuildVRT can mosaic them losslessly.

        Writing them as EPSG:4326 with a linearly-interpolated latitude step
        (the previous implementation) is wrong: pixel rows are evenly spaced in
        Mercator y, not in latitude. Peak error inside a single z10 tile at
        40 degrees N is about 14.8 m.

        Args:
            tile: Tile object with zoom/x/y
            width: Tile image width in pixels
            height: Tile image height in pixels

        Returns:
            (geotransform, epsg_code) where geotransform is
            [top_left_x, pixel_width, 0, top_left_y, 0, pixel_height]
        """
        # Half-circumference of the earth at the equator, in metres.
        origin = 20037508.342789244
        tile_span = 2 * origin / (2 ** tile.zoom)

        x0 = -origin + tile.x * tile_span
        y0 = origin - tile.y * tile_span

        geotransform = [x0, tile_span / width, 0, y0, 0, -tile_span / height]
        return geotransform, 3857
```

- [ ] **Step 2: 跑测试确认转绿**

```bash
uv run pytest tests/test_tile_georeference.py -v
```

期望：两个测试都 PASS。第二个测试现在走 `epsg == 3857` 分支，最大偏差应 < 0.01 m（纯浮点误差）。

- [ ] **Step 3: 跑全量测试**

```bash
uv run pytest tests/ -q
```

期望：**150 passed, 0 failed**。

如果 `test_vrt_builder.py` 或其他拼接相关测试失败，**不要改测试来迁就**——先读失败信息判断是测试写死了 4326 假设，还是实现真的坏了。

- [ ] **Step 4: 提交**

```bash
git add services/download_engine.py
git commit -m "fix(download): 瓦片配准改用 EPSG:3857 平面坐标,修正 14.8m 偏差

瓦片像素行在纵向是 Web Mercator y 等间隔,原实现把 (lat_max-lat_min)
线性均分给 256 行,当成 EPSG:4326 写出。四角坐标对、中间像素全错,
z10@40N 瓦片内峰值 14.8m、z6 达 3.7km。

更糟的是 pixel_height 随纬度变化(同 zoom 下不同 y 的瓦片纬度跨度不同),
BuildVRT 拼接时按平均分辨率重采样,接缝处再叠一层错位。

改用 3857 平面坐标后全 zoom 内像素大小恒定,BuildVRT 无损。
输出坐标系的兼容处理见下一个 commit。"
```

**Risks:**
- **此时产出的 GeoTIFF 坐标系已变成 EPSG:3857**，与存量行为不一致。Task 4 会补上回到 4326 的重投影。**Task 3 和 Task 4 之间不要发版。**
- `origin` 值 20037508.342789244 = `6378137 * π`，是 Web Mercator 的标准边界，不要写成 20037508.34（精度不够会在高 zoom 累积）

---

## Task 4：拼接末尾重投影回 EPSG:4326，并清掉配准缓存

**Files:**
- Modify: `services/download_engine.py:640-725`（`stitch_tiles_with_gdal` 的 VRT→Translate 段）
- Test: `tests/test_tile_georeference.py`（追加）

**Interfaces:**
- Consumes: `tile_geotransform` 返回 3857（Task 3）
- Produces: `stitch_tiles_with_gdal(tiles, style, output_path, zoom_level, target_epsg=4326)` —— 新增可选参数。

**Rationale:** 内部用 3857 保证拼接无损，但存量用户拿到的一直是 4326，不能静默改变。用 `gdal.Warp` 做一次**真正的重投影**（而不是原来那种错误的线性近似）输出 4326。

- [ ] **Step 1: 写测试锁定默认输出坐标系**

在 `tests/test_tile_georeference.py` 追加：

```python
def test_stitch_default_target_epsg_is_4326():
    """默认输出必须仍是 EPSG:4326,不能静默改变存量用户的产出"""
    import inspect
    from services.download_engine import DownloadEngine

    sig = inspect.signature(DownloadEngine.stitch_tiles_with_gdal)
    assert 'target_epsg' in sig.parameters, "stitch_tiles_with_gdal 应有 target_epsg 参数"
    assert sig.parameters['target_epsg'].default == 4326, "默认输出坐标系必须保持 4326"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
uv run pytest tests/test_tile_georeference.py::test_stitch_default_target_epsg_is_4326 -v
```

期望：`AssertionError: stitch_tiles_with_gdal 应有 target_epsg 参数`

- [ ] **Step 3: 给 stitch_tiles_with_gdal 加 target_epsg 参数**

找到 `stitch_tiles_with_gdal` 的定义（在 `download_engine.py` 中，`gdal_compression = ...` 那行之前），在参数列表末尾加 `target_epsg: int = 4326`。

然后把 663-701 行（BuildVRT 到 Translate 那段）替换为：

```python
        vrt_ds = gdal.BuildVRT(vrt_path, georef_paths, options=vrt_options)
        if vrt_ds is None:
            raise RuntimeError(f"Failed to build VRT from {len(georef_paths)} tiles")
        vrt_ds = None  # Close VRT dataset

        logger.info("VRT built successfully")

        # Reproject to the requested CRS if it differs from the tile CRS (3857).
        # Tiles are georeferenced in EPSG:3857 (see tile_geotransform); a real
        # gdal.Warp is required to get correct EPSG:4326 output.
        warped_path = None
        translate_source = vrt_path
        if target_epsg != 3857:
            warped_path = str(output_path_obj.with_suffix('.warp.vrt'))
            logger.info(f"Warping VRT to EPSG:{target_epsg}: {warped_path}")
            warp_ds = gdal.Warp(
                warped_path,
                vrt_path,
                format='VRT',
                dstSRS=f'EPSG:{target_epsg}',
                resampleAlg=gdal_resampling,
            )
            if warp_ds is None:
                raise RuntimeError(f"Failed to warp VRT to EPSG:{target_epsg}")
            warp_ds = None
            translate_source = warped_path

        # Translate VRT to final format
        logger.info(f"Translating VRT to final format: {output_path}")

        # Determine output format based on file extension
        output_ext = Path(output_path).suffix.lower()
        if output_ext == '.tif' or output_ext == '.tiff':
            output_format = 'GTiff'
            translate_options = gdal.TranslateOptions(
                format=output_format,
                creationOptions=[f'COMPRESS={gdal_compression}', 'TILED=YES'],
                resampleAlg=gdal_resampling
            )
        elif output_ext == '.png':
            output_format = 'PNG'
            translate_options = gdal.TranslateOptions(
                format=output_format,
                resampleAlg=gdal_resampling
            )
        else:
            # Default to GeoTIFF
            output_format = 'GTiff'
            translate_options = gdal.TranslateOptions(
                format=output_format,
                creationOptions=[f'COMPRESS={gdal_compression}', 'TILED=YES'],
                resampleAlg=gdal_resampling
            )

        # Perform translation
        output_ds = gdal.Translate(str(output_path_obj), translate_source, options=translate_options)
        if output_ds is None:
            raise RuntimeError(f"Failed to translate VRT to {output_path_obj}")
        output_ds = None  # Close output dataset

        logger.info(f"Translation completed: {output_path_obj}")
```

再在清理段（原 707 行 `vrt_path_obj.unlink()` 那个 try 之后）追加 warp 中间文件的清理：

```python
        # 1b. Clean up warped VRT if one was created
        if warped_path:
            try:
                Path(warped_path).unlink()
                logger.debug(f"Cleaned up warped VRT: {warped_path}")
            except Exception as e:
                logger.warning(f"Failed to clean up warped VRT {warped_path}: {e}")
```

- [ ] **Step 4: 跑测试**

```bash
uv run pytest tests/test_tile_georeference.py -v
uv run pytest tests/ -q
```

期望：3 个新测试全 PASS，全量 **151 passed**。

- [ ] **Step 5: 手工验证真实产物（必做，不能跳过）**

```bash
# 1. 必须先清掉旧的配准缓存,否则 _add_georeference:766 的 exists() 短路会复用旧文件
find cache/ -name '*_geo.tif' -delete

# 2. 起服务,建一个小任务(zoom 10-11,小范围),等它跑完拼接
DEBUG=0 uv run python app.py

# 3. 用 gdalinfo 检查产物
uv run python -c "
from osgeo import gdal
ds = gdal.Open('downloads/map/task_<ID>/<NAME>_zoom_10.tif')
print('投影:', ds.GetProjection()[:80])
print('geotransform:', ds.GetGeoTransform())
"
```

期望：投影字符串含 `WGS 84` / `EPSG","4326`；geotransform 的 `[1]`（pixel_width）与 `[5]`（pixel_height）绝对值接近相等（4326 下 warp 出来的像素近似方形）。

- [ ] **Step 6: 提交**

```bash
git add services/download_engine.py tests/test_tile_georeference.py
git commit -m "fix(download): 拼接末尾用 gdal.Warp 真正重投影回 EPSG:4326

上一个 commit 把瓦片配准改成 3857 后,产出坐标系与存量行为不一致。
本次在 BuildVRT 之后、Translate 之前插入 gdal.Warp,把默认输出重新
投影回 4326——这次是真正的重采样重投影,而不是原来那种线性近似。

新增 target_epsg 参数(默认 4326)。传 3857 可拿到无重采样的原生投影
产物,但本阶段不做 UI 暴露。

注意:_add_georeference:766 有 exists() 缓存短路,验证前必须
find cache/ -name '*_geo.tif' -delete,否则复用旧文件导致修复静默失效。"
```

**Risks:**
- **配准缓存短路是本任务最容易踩的坑。** 忘了清 `*_geo.tif` 会让你以为修复没生效，白白排查半天
- `gdal.Warp` 输出 `format='VRT'` 是为了不落地中间大文件，最终仍由 `Translate` 写实体文件
- 重投影会引入重采样，`gdal_resampling` 默认 `nearest`（配置项 `gdal_resampling`）。对影像瓦片 nearest 会有轻微锯齿，但保持与原行为一致，**本阶段不改默认值**

---

## Task 5：补全 OutputFormat 枚举

**Files:**
- Modify: `models/task.py:69-95`
- Test: `tests/test_output_format.py`（新建）

**Interfaces:**
- Consumes: 无
- Produces: `OutputFormat.IMAGE_ONLY = "image_only"`；`from_shorthand('image_only')` 与 `from_shorthand('i')` 均返回 `'image_only'`。Task 6 依赖。

**Rationale:** `templates/index.html:91` 提供 `image_only` 选项，但枚举里没有，`from_shorthand` 对未知值直接 `raise ValueError`。实跑报错原文：`ValueError: output_format (image_only) must be one of ['p', 'j', 'b', 't', 'png', 'jpg', 'both', 'tiles_only']`，被 `routes/api.py:87` catch 成 HTTP 400，用户看到 toast 报错。

`models/task.py` 目前**零测试覆盖**（grep `OutputFormat` 在测试里只作为 SQL 列名出现）。

- [ ] **Step 1: 写失败测试**

创建 `tests/test_output_format.py`：

```python
"""
OutputFormat enum and semantics tests
"""

import os
import sys

import pytest

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from models.task import OutputFormat


def test_image_only_is_a_valid_format():
    """index.html 提供了 image_only 选项,枚举必须认它"""
    assert OutputFormat.from_shorthand('image_only') == 'image_only'


def test_image_only_shorthand():
    assert OutputFormat.from_shorthand('i') == 'image_only'


def test_existing_formats_still_work():
    """回归保护:原有四个值不能被破坏"""
    assert OutputFormat.from_shorthand('both') == 'both'
    assert OutputFormat.from_shorthand('b') == 'both'
    assert OutputFormat.from_shorthand('tiles_only') == 'tiles_only'
    assert OutputFormat.from_shorthand('t') == 'tiles_only'
    assert OutputFormat.from_shorthand('png') == 'png'
    assert OutputFormat.from_shorthand('jpg') == 'jpg'


def test_unknown_format_still_raises():
    with pytest.raises(ValueError):
        OutputFormat.from_shorthand('definitely_not_a_format')
```

- [ ] **Step 2: 跑测试确认失败**

```bash
uv run pytest tests/test_output_format.py -v
```

期望：前两个 FAIL，报 `ValueError: output_format (image_only) must be one of [...]`；后两个 PASS。

- [ ] **Step 3: 补枚举成员与简写**

`models/task.py:69-74`，在 `TILES_ONLY` 之后加一行：

```python
class OutputFormat(Enum):
    """Output format enumeration"""
    PNG = "png"
    JPG = "jpg"
    BOTH = "both"
    TILES_ONLY = "tiles_only"  # Only download tiles, no merging
    IMAGE_ONLY = "image_only"  # Only stitched image, do not copy tiles out
```

`models/task.py:90-95` 的 `shorthand_map` 加一项：

```python
        shorthand_map = {
            'p': 'png',
            'j': 'jpg',
            'b': 'both',
            't': 'tiles_only',
            'i': 'image_only',
        }
```

- [ ] **Step 4: 跑测试确认全绿**

```bash
uv run pytest tests/test_output_format.py -v
uv run pytest tests/ -q
```

期望：5 个新测试全 PASS，全量 **156 passed**。

- [ ] **Step 5: 提交**

```bash
git add models/task.py tests/test_output_format.py
git commit -m "fix(model): OutputFormat 补 IMAGE_ONLY,修复「仅拼接图」必崩

index.html:91 一直提供 image_only 选项,但 OutputFormat 枚举里没有,
from_shorthand 对未知值直接 raise ValueError,被 routes/api.py:87
catch 成 400,用户看到报错 toast。2026-06-11 已记录过此问题。

models/task.py 此前零测试覆盖,本次补上枚举校验测试。
三种格式的语义差异见下一个 commit。"
```

---

## Task 6：修正 both / tiles_only / image_only 三种语义

**Files:**
- Modify: `services/task_manager.py:875`、`:936`
- Test: `tests/test_output_format.py`（追加）

**Interfaces:**
- Consumes: `OutputFormat.IMAGE_ONLY`（Task 5）
- Produces: 无

**Rationale:** 光补枚举不够——当前 `:875` 的 `if task.output_format in ['png','jpg','both']` 只拼接，`:936` 的 `elif task.output_format == 'tiles_only'` 只复制，两者 **if/elif 互斥**。所以 `both` 今天的行为是「只拼接，不复制瓦片」，与它的名字不符。若只补枚举，`both` 和 `image_only` 产出物会完全一样。

正确语义：

| 值 | 拼接 GeoTIFF | 复制瓦片到 output_path |
|---|---|---|
| `both` | ✅ | ✅ |
| `image_only`（含遗留的 `png`/`jpg`） | ✅ | ❌ |
| `tiles_only` | ❌ | ✅ |

- [ ] **Step 1: 写语义契约测试**

在 `tests/test_output_format.py` 追加：

```python
STITCH_FORMATS = {'both', 'image_only', 'png', 'jpg'}
COPY_FORMATS = {'both', 'tiles_only'}


def _task_manager_source():
    root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
    with open(os.path.join(root, 'services', 'task_manager.py'), encoding='utf-8') as f:
        return f.read()


def test_stitch_branch_covers_all_stitch_formats():
    """拼接分支必须覆盖 both/image_only/png/jpg 四个值"""
    src = _task_manager_source()
    for fmt in STITCH_FORMATS:
        assert f"'{fmt}'" in src, f"task_manager.py 中找不到拼接格式 {fmt}"


def test_both_copies_tiles_and_stitches():
    """
    both 必须同时拼接和复制。

    这是文本级契约测试:确认复制分支的条件不再是排他的 elif ... == 'tiles_only',
    而是能同时被 both 命中。
    """
    src = _task_manager_source()
    assert "elif task.output_format == 'tiles_only':" not in src, (
        "复制瓦片仍是排他 elif,both 永远走不到复制分支"
    )
    assert "task.output_format in ['both', 'tiles_only']" in src, (
        "复制分支应改为 in ['both', 'tiles_only']"
    )
```

- [ ] **Step 2: 跑测试确认失败**

```bash
uv run pytest tests/test_output_format.py -v
```

期望：`test_stitch_branch_covers_all_stitch_formats` FAIL（找不到 `'image_only'`），`test_both_copies_tiles_and_stitches` FAIL（排他 elif 仍在）。

- [ ] **Step 3: 改拼接分支条件**

`services/task_manager.py:875`：

```python
            # Stitch tiles if output format includes image
            if task.output_format in ['png', 'jpg', 'both', 'image_only']:
```

- [ ] **Step 4: 把复制分支从 elif 改成独立 if**

`services/task_manager.py:936`，把：

```python
            # Handle tiles_only format: copy tiles to output_path
            elif task.output_format == 'tiles_only':
```

改为：

```python
            # Copy tiles to output_path for formats that keep the raw tiles.
            # NOTE: this is a separate `if`, not `elif` — 'both' must do both.
            if task.output_format in ['both', 'tiles_only']:
```

- [ ] **Step 5: 跑测试**

```bash
uv run pytest tests/test_output_format.py -v
uv run pytest tests/ -q
```

期望：全绿，全量 **158 passed**。

- [ ] **Step 6: 提交**

```bash
git add services/task_manager.py tests/test_output_format.py
git commit -m "fix(task): 理清 both/tiles_only/image_only 三种输出语义

原本 :875 拼接与 :936 复制是 if/elif 互斥,导致 both 只拼接不复制瓦片,
与名字不符;补了 image_only 枚举后两者产出物还会完全一样。

改为:
- both       拼接 + 复制
- image_only 只拼接(遗留的 png/jpg 作为同义词兼容)
- tiles_only 只复制

注意 png/jpg 对产物没有实际影响:task_manager.py:909 输出路径硬编码
.tif,download_engine.py:674-676 按扩展名选驱动,永远走 GTiff。它们只
作为 image_only 的同义词保留,不声称支持 PNG/JPG 输出。"
```

**Risks:**
- 改成两个独立 `if` 后，`both` 会**同时**在缓存和 `output_path` 各存一份瓦片，**磁盘占用翻倍**。这是符合 `both` 语义的正确行为，但大任务下值得注意。如果用户反馈磁盘吃紧，那是另一个话题（加清理策略），不要在本任务里顺手处理
- 第二个测试是文本级断言，比较脆——它锁定的是具体写法。如果后续重构了这段分支，测试会失败，届时应更新测试而不是回退实现

---

## Task 7：前后端输出格式一致性回归测试

**Files:**
- Test: `tests/test_output_format.py`（追加）

**Interfaces:**
- Consumes: `OutputFormat`（Task 5）
- Produces: 无

**Rationale:** 这次的 bug 根源是「前端下拉框的值」和「后端枚举」各改各的，没人保证它们一致。加一个测试锁死这个契约，防止复发。范式参照 `tests/test_index_has_contour_option.py`。

- [ ] **Step 1: 写契约测试**

在 `tests/test_output_format.py` 追加：

```python
import re


def test_every_option_in_index_html_is_a_valid_output_format():
    """
    index.html 的 #outputFormat 下拉里每个 value,后端枚举都必须认。

    这个测试存在的原因:image_only 这个 bug 就是前端加了选项、后端枚举
    没跟上造成的。
    """
    root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
    with open(os.path.join(root, 'templates', 'index.html'), encoding='utf-8') as f:
        html = f.read()

    match = re.search(
        r'<select[^>]*id="outputFormat"[^>]*>(.*?)</select>',
        html,
        re.S,
    )
    assert match, "index.html 里找不到 #outputFormat 下拉框"

    values = re.findall(r'<option\s+value="([^"]+)"', match.group(1))
    assert values, "#outputFormat 下拉框里没有 option"

    for value in values:
        # 不认的值会 raise ValueError,测试即失败
        OutputFormat.from_shorthand(value)
```

- [ ] **Step 2: 跑测试确认通过**

```bash
uv run pytest tests/test_output_format.py::test_every_option_in_index_html_is_a_valid_output_format -v
```

期望：**PASS**（Task 5 已经补了枚举）。

> 这个测试写在修复**之后**是有意为之——它是防复发的护栏，不是驱动实现的红灯。想验证它真的有效，可以临时把 `models/task.py` 的 `IMAGE_ONLY` 注释掉，跑一次看它变红，再恢复。

- [ ] **Step 3: 跑全量并提交**

```bash
uv run pytest tests/ -q
git add tests/test_output_format.py
git commit -m "test(model): 锁死 index.html 输出格式下拉与后端枚举的一致性

image_only 这个 bug 的根源是前端加选项、后端枚举没跟上。加契约测试,
任何一边单独改动都会红灯。"
```

---

## Task 8：抽出表单重置公共函数，修掉 finally 覆盖 disabled

**Files:**
- Modify: `static/js/map.js`（三处重置逻辑、三处 finally、`apply()` 的按钮状态）
- Test: `tests/test_map_js_contract.py`（新建）

**Interfaces:**
- Consumes: 无
- Produces: `resetForm({clearBounds = true} = {})` 与 `refreshSubmitButtonState()` 两个全局函数。Task 9 依赖 `refreshSubmitButtonState`。

**Rationale:** 三处重复的重置逻辑，且三处 `finally` 都在 reset 之后无条件 `btn.disabled = false`，**覆盖掉 reset 想要的 disabled 状态**。另外 `map.js:122-123` 的 `if (btn && isLocal) btn.disabled = false;` 只加不减——切到「本地高程切片」解禁按钮后再切回「地图瓦片」，此时没有 bbox 但按钮仍可点。

- [ ] **Step 1: 写契约测试**

创建 `tests/test_map_js_contract.py`：

```python
"""
map.js behavioural contract tests (text-level regression guards).

本项目没有 JS 测试框架(无 package.json/vitest,且不打算引入——会破坏
PyInstaller 离线打包形态)。这些测试用文本断言守住关键契约,真实行为
由 playwright 手工实测覆盖(见计划 Task 10)。
"""

import os
import sys

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))


def _map_js():
    root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
    with open(os.path.join(root, 'static', 'js', 'map.js'), encoding='utf-8') as f:
        return f.read()


def test_reset_form_helper_exists():
    """三处重复的表单重置逻辑必须收敛成一个函数"""
    src = _map_js()
    assert 'function resetForm(' in src, "map.js 应定义 resetForm()"


def test_submit_button_state_is_centralised():
    """按钮启用/禁用必须走统一函数,避免只加不减"""
    src = _map_js()
    assert 'function refreshSubmitButtonState(' in src, (
        "map.js 应定义 refreshSubmitButtonState()"
    )
    assert 'if (btn && isLocal) btn.disabled = false;' not in src, (
        "apply() 里只加不减的按钮解禁应改为走 refreshSubmitButtonState()"
    )
```

- [ ] **Step 2: 跑测试确认失败**

```bash
uv run pytest tests/test_map_js_contract.py -v
```

期望：两个都 FAIL（`map.js` 应定义 resetForm()）。

- [ ] **Step 3: 加两个公共函数**

在 `static/js/map.js` 的 `updateBoundsInfo()` 函数**之前**（第 138 行前）插入：

```javascript
// 提交按钮的启用条件集中在这里，避免各处只加不减导致状态残留。
// 本地高程切片模式没有 bbox，只要选了文件就能提交；其余模式必须先框选。
function refreshSubmitButtonState() {
    const btn = document.getElementById('createTaskBtn');
    if (!btn) return;
    const type = document.getElementById('downloadType')?.value;
    if (type === 'local_terrain') {
        btn.disabled = false;
    } else {
        btn.disabled = !currentBounds;
    }
}

// 任务创建成功后复位表单。
// clearBounds=false 用于本地高程切片：该模式本来就没有 bbox，
// 清空 drawnItems 会把用户为下一个任务画好的框也一起删掉。
function resetForm({ clearBounds = true } = {}) {
    const form = document.getElementById('downloadForm');
    if (form) form.reset();

    const outputPath = document.getElementById('outputPath');
    if (outputPath) delete outputPath.dataset.userEdited;

    if (clearBounds) {
        if (drawnItems) drawnItems.clearLayers();
        currentBounds = null;
        updateBoundsInfo();
    }

    // 让 apply() 重新按当前类型摆好字段可见性和默认路径
    const typeEl = document.getElementById('downloadType');
    if (typeEl) typeEl.dispatchEvent(new Event('change'));

    refreshSubmitButtonState();
}
```

- [ ] **Step 4: 把 apply() 里只加不减的按钮逻辑换掉**

`static/js/map.js:122-123`，把：

```javascript
        const btn = document.getElementById('createTaskBtn');
        if (btn && isLocal) btn.disabled = false;
```

替换为：

```javascript
        refreshSubmitButtonState();
```

- [ ] **Step 5: 三处提交分支改用 resetForm，并修掉 finally 覆盖**

在 `map.js` 中找到三处任务创建成功后的重置代码（分别在地图瓦片/DEM 分支、等高线分支、本地高程分支，原先各有一段 `form.reset()` 等语句），全部替换：

- 地图瓦片 / DEM / 等高线分支 → `resetForm();`
- 本地高程切片分支 → `resetForm({ clearBounds: false });`

然后检查这三个分支的 `finally` 块。原先形如：

```javascript
        } finally {
            btn.disabled = false;
            btn.innerHTML = originalHtml;
        }
```

必须改为：

```javascript
        } finally {
            btn.innerHTML = originalHtml;
            refreshSubmitButtonState();
        }
```

**关键：删掉 `btn.disabled = false;`**，改为调用 `refreshSubmitButtonState()`。原来那行会在 `resetForm()` 之后无条件解禁按钮，让重置形同虚设。

- [ ] **Step 6: 跑测试**

```bash
uv run pytest tests/test_map_js_contract.py -v
uv run pytest tests/ -q
```

期望：全绿，全量 **160 passed**。

- [ ] **Step 7: 提交**

```bash
git add static/js/map.js tests/test_map_js_contract.py
git commit -m "fix(map): 抽出 resetForm/refreshSubmitButtonState,修表单重置失效

三处重复的重置逻辑,且三处 finally 都在 reset 之后无条件
btn.disabled = false,把重置想要的 disabled 状态覆盖掉,重置形同虚设。

顺带修掉 apply() 里 if (btn && isLocal) btn.disabled = false 只加不减:
切到本地高程切片解禁按钮后再切回地图瓦片,此时没有 bbox 按钮却仍可点,
点了只会弹「请先在地图上框选下载区域」。

本地高程分支保持不清 drawnItems(该模式无 bbox),通过
resetForm({clearBounds:false}) 表达。

已知未处理:outputPath.dataset.userEdited 的清除时机语义(现在 resetForm
会清掉它,与原行为一致)。"
```

**Risks:**
- 本地高程分支**必须**传 `{clearBounds: false}`。一刀切会清掉用户为下一个任务画好的框
- `typeEl.dispatchEvent(new Event('change'))` 会触发 `apply()`，而 `apply()` 现在调 `refreshSubmitButtonState()` —— 确认没有递归（`refreshSubmitButtonState` 不再触发 change，安全）

---

## Task 9：监听 Leaflet.draw 编辑事件，拖拽改框后同步 bbox

**Files:**
- Modify: `static/js/map.js:41-77`（`initMap` 内的事件监听）
- Test: `tests/test_map_js_contract.py`（追加）

**Interfaces:**
- Consumes: `refreshSubmitButtonState()`（Task 8）
- Produces: 无

**Rationale:** `map.js:34-37` 开了编辑工具栏，但只监听 `CREATED`(:41) 和 `DELETED`(:72)，**全项目 grep `L.Draw.Event.EDITED` 零命中**。用户拖顶点改框 → 地图矩形变了 → 右侧四至不变 → 提交旧 bbox。

**事件时序已核实**（逐行核对 leaflet.draw 1.0.4 源码）：

| 事件 | 常量值 | 何时触发 | payload |
|---|---|---|---|
| `EDITRESIZE` | `draw:editresize` | 拖矩形四角把手时实时触发 | `{layer}` |
| `EDITMOVE` | `draw:editmove` | 整体拖动图形时实时触发 | `{layer}` |
| `EDITED` | `draw:edited` | 点「保存」时 | `{layers}`（FeatureGroup） |
| `EDITSTOP` | `draw:editstop` | 退出编辑模式时（保存和取消**都会**触发） | — |

取消编辑的路径是 `disable()` → `revertLayers()` 还原图形 → **之后**才 fire `EDITSTOP`。所以在 `EDITSTOP` 里重新读 `drawnItems` 的 bounds，对保存和取消**都正确**。

- [ ] **Step 1: 写契约测试**

在 `tests/test_map_js_contract.py` 追加：

```python
def test_edit_events_are_wired():
    """
    编辑工具栏开着(edit.featureGroup),就必须监听编辑事件,
    否则用户拖拽改框后提交的还是旧 bbox。
    """
    src = _map_js()
    assert 'L.Draw.Event.EDITED' in src, "未监听 draw:edited,保存编辑后 bbox 不更新"
    assert 'L.Draw.Event.EDITRESIZE' in src, "未监听 draw:editresize,拖角时四至不实时更新"
    assert 'L.Draw.Event.EDITSTOP' in src, "未监听 draw:editstop,取消编辑后 bbox 不还原"


def test_bounds_sync_helper_exists():
    src = _map_js()
    assert 'function syncBoundsFromDrawnItems(' in src, (
        "map.js 应定义 syncBoundsFromDrawnItems()"
    )
```

- [ ] **Step 2: 跑测试确认失败**

```bash
uv run pytest tests/test_map_js_contract.py -v
```

期望：两个新测试 FAIL（`未监听 draw:edited...`）。

- [ ] **Step 3: 加同步函数**

在 `static/js/map.js` 的 `refreshSubmitButtonState()` 之前插入：

```javascript
// 从 drawnItems 里当前的图层重新读取 bbox。
// 编辑（拖角/拖动/保存/取消）之后统一走这里，保证右侧四至和地图上看到的一致。
function syncBoundsFromDrawnItems() {
    let found = null;
    if (drawnItems) {
        drawnItems.eachLayer(function (layer) {
            if (typeof layer.getBounds === 'function') {
                found = layer.getBounds();
            }
        });
    }

    if (found) {
        currentBounds = {
            north: found.getNorth(),
            south: found.getSouth(),
            east: found.getEast(),
            west: found.getWest()
        };
    } else {
        currentBounds = null;
    }

    updateBoundsInfo();
    refreshSubmitButtonState();
}
```

- [ ] **Step 4: 挂上事件监听**

在 `static/js/map.js` 的 `map.on(L.Draw.Event.DELETED, ...)` 之后（原第 76 行 `});` 之后、`initMap` 的收尾 `}` 之前）插入：

```javascript
    // 拖角 / 整体拖动时实时同步，用户不必点保存就能看到四至变化
    map.on(L.Draw.Event.EDITRESIZE, syncBoundsFromDrawnItems);
    map.on(L.Draw.Event.EDITMOVE, syncBoundsFromDrawnItems);

    // 点「保存」后确认一次
    map.on(L.Draw.Event.EDITED, syncBoundsFromDrawnItems);

    // 退出编辑模式。leaflet.draw 1.0.4 在取消时先 revertLayers() 还原图形、
    // 之后才 fire EDITSTOP，所以这里重读 bounds 对保存和取消都正确。
    map.on(L.Draw.Event.EDITSTOP, syncBoundsFromDrawnItems);

    // 删除模式结束后同样重读（DELETED 只在真的删了东西时触发）
    map.on(L.Draw.Event.DELETESTOP, syncBoundsFromDrawnItems);
```

- [ ] **Step 5: 跑测试**

```bash
uv run pytest tests/test_map_js_contract.py -v
uv run pytest tests/ -q
```

期望：全绿，全量 **162 passed**。

- [ ] **Step 6: 提交**

```bash
git add static/js/map.js tests/test_map_js_contract.py
git commit -m "fix(map): 监听 Leaflet.draw 编辑事件,拖拽改框后同步 bbox

map.js:34-37 开着编辑工具栏,却只监听 CREATED 和 DELETED,全项目
grep L.Draw.Event.EDITED 零命中。用户拖顶点把框改小、点保存,地图上
矩形确实变了,右侧四至数字纹丝不动,提交的还是旧 bbox——静默下错范围。

监听 EDITRESIZE/EDITMOVE(实时) + EDITED(保存) + EDITSTOP/DELETESTOP
(退出模式),统一走 syncBoundsFromDrawnItems() 重读。

时序已逐行核对 leaflet.draw 1.0.4 源码:取消编辑走 disable() →
revertLayers() 还原 → 之后才 fire EDITSTOP,所以在 EDITSTOP 里重读
bounds 对保存和取消都正确。"
```

**Risks:**
- `drawnItems.eachLayer` 遍历取**最后一个**有 `getBounds` 的图层。当前 `CREATED` 分支会先 `clearLayers()`，所以永远只有一个图层，安全。如果将来支持多选区，这里要改
- `DELETESTOP` 与 `DELETED` 会**都触发**（删除后点保存）。`syncBoundsFromDrawnItems` 是幂等的，重复调用无害

---

## Task 10：Playwright 实测验证真实交互行为

**Files:**
- 无代码改动（纯验证任务）

**Interfaces:**
- Consumes: Task 8、Task 9 的全部改动
- Produces: 无

**Rationale:** Task 8/9 的 pytest 测试是**文本级**的，只能证明"代码里写了这些字符串"，不能证明"用户拖拽后数字真的变了"。真实行为必须实测。

- [ ] **Step 1: 起服务**

```bash
find cache/ -name '*_geo.tif' -delete
DEBUG=0 uv run python app.py
```

**必须用 `DEBUG=0`** —— 默认 `DEBUG=1` 会开 reloader 起两个进程，收尾 kill 麻烦。

- [ ] **Step 2: 验证拖拽改框同步（Task 9）**

浏览器打开 `http://127.0.0.1:5000/`，然后：

1. 点地图左上角的矩形工具（`a.leaflet-draw-draw-rectangle`），在地图上拖出一个框
2. **记下**右侧 `#boundsInfo` 里的四个数字
3. 点编辑按钮（`a.leaflet-draw-edit-edit`）
4. 拖动矩形任意一个角的把手（`.leaflet-editing-icon.leaflet-edit-resize`）
5. **期望：还没点保存，右侧四至数字就已经跟着变了**（EDITRESIZE 实时同步）
6. 点「保存」（`ul.leaflet-draw-actions` 里的第一个链接）
7. **期望：数字保持第 5 步的新值**
8. 再次进入编辑，拖动把手，这次点「取消」
9. **期望：地图上矩形还原成原样，且右侧数字也还原**（EDITSTOP 重读）

- [ ] **Step 3: 验证表单重置（Task 8）**

1. 「下载类型」选「本地高程切片（上传 GeoTIFF）」
2. **期望：「创建下载任务」按钮变为可点**
3. 切回「地图瓦片」，且此时**没有**框选区域（先用垃圾桶删掉框）
4. **期望：按钮变回禁用**（这是修复前的 bug：只加不减，按钮会一直可点）
5. 画一个框，填任务名，提交创建一个 zoom 10-10 的小任务
6. **期望：创建成功后表单被重置（任务名清空、框消失、按钮回到禁用）**

- [ ] **Step 4: 验证配准修复的真实产物（Task 3/4）**

等第 3 步创建的任务跑完拼接，然后：

```bash
uv run python -c "
from osgeo import gdal
import glob
f = glob.glob('downloads/map/task_*/*_zoom_10.tif')[-1]
ds = gdal.Open(f)
gt = ds.GetGeoTransform()
print('文件:', f)
print('投影:', ds.GetProjection()[:100])
print('geotransform:', gt)
print('像素宽高比:', abs(gt[1] / gt[5]))
"
```

期望：
- 投影含 `WGS 84` 或 `EPSG","4326"`
- 「像素宽高比」接近 1.0（在 40°N 附近，4326 重投影后像素近似方形）

- [ ] **Step 5: 收尾**

```bash
# 停掉服务（Ctrl+C），确认没有残留进程
curl -s -o /dev/null -w "%{http_code}\n" --max-time 3 http://127.0.0.1:5000/ || echo "已停止"
```

- [ ] **Step 6: 记录实测结果**

把第 2/3/4 步的实际观察结果写进本任务的复选框旁边。**如果任何一步不符合期望，不要标记完成** —— 回到对应的 Task 重新修。

**Risks:**
- **本任务的 Playwright 脚本未经实跑验证**。选择器已核实存在于 leaflet.draw 1.0.4 的源码和 CSS 里，但拖拽的坐标偏移量需要按实际视口调整
- 第 4 步依赖真实下载。如果 WSL 环境无代理导致 Google 瓦片下不来，先在配置页设好代理（项目记忆：WSL2 下需要 `export HTTPS_PROXY` 指向 Windows 网关）

---

## 阶段收尾

全部 10 个任务完成后：

- [ ] `uv run pytest tests/ -q` → **162 passed, 0 failed**
- [ ] 主计划 [Phase 1 验收标准](2026-07-27-master-plan.md#phase-1--2026-07-27-phase1-datamd) 逐条勾选
- [ ] **不 bump 版本号、不发版** —— 用户已决定全部改造（Phase 1 + 调色板修复 + Phase 2）完成后统一发一个版本。本阶段完成后合并到集成分支即可，版本号留到最后一次性 bump。
